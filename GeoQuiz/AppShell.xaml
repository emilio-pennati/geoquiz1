<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="GeoQuiz.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:GeoQuiz.Views"
    Title="GeoQuiz Nations">

    <TabBar>
        <Tab Title="Search" Icon="search.png">
            <ShellContent ContentTemplate="{DataTemplate views:SearchPage}" />
        </Tab>
        <Tab Title="Quiz" Icon="quiz.png">
            <ShellContent ContentTemplate="{DataTemplate views:QuizPage}" />
        </Tab>
        <Tab Title="Stats" Icon="stats.png">
            <ShellContent ContentTemplate="{DataTemplate views:StatsPage}" />
        </Tab>
        <Tab Title="Settings" Icon="settings.png">
            <ShellContent ContentTemplate="{DataTemplate views:SettingsPage}" />
        </Tab>
    </TabBar>

</Shell>