# Iteration 03 - Schermata Search

## Objective
Implementare SearchPage con SearchBar e CollectionView per la ricerca dei Paesi

## Plan
- Implementazione SearchPage con SearchBar e CollectionView
- ViewModel con logica ricerca
- Binding dati bandiere
- Gestione stati: loading, error, empty, data

## Main Prompts Used
1. "crea prima un branch"
2. "procedi all'implementazione della terza iterazione"
3. "genera la documentazione e fai la commit docs"

## Files Created
- GeoQuiz/ViewModels/SearchViewModel.cs - ViewModel con ricerca, filtering, gestione stati (IsBusy, ErrorMessage, HasData, IsEmptyState)
- GeoQuiz/ViewModels/CountryListItem.cs - Modello per item lista (Code, Name, FlagUrl, Capital, Region)
- GeoQuiz/Views/SearchPage.xaml - SearchBar + CollectionView con binding bandiere, nomi, capitali, regioni
- GeoQuiz/Views/SearchPage.xaml.cs - Code-behind con dependency injection e evento Loaded per caricamento iniziale

## Files Modified
- GeoQuiz/MauiProgram.cs - Registrazione SearchPage con dependency injection

## AI Code Accepted
- Tutto il codice ViewModel e View
- Pattern MVVM con CommunityToolkit.Mvvm
- Gestione errori HTTP

## Tests Executed
- [x] Build: dotnet build - completato con successo (warning: Frame obsoleto, usare Border in futuro)

## Issues Found
- Warning: Frame è obsoleto in .NET 9, dovrebbe essere sostituito con Border
- Warning: campi ObservableProperty non compatibili AOT in WinRT (possibile fix futuro)

## Outcome
Completed - SearchPage funzionante con ricerca e visualizzazione bandiere

## Fixes Applied
- CountryService: ridotti campi API da 12 a 10 (rimossi timezones, borders per rispettare limite REST Countries API)
- CountryService: timeout aumentato da 10s a 30s
- CountryService: rimosso fallback a sample data
- CountryDto: fix naming `string?Svg` → `string? Svg`

## Commit History
- 3fddba8 fix: remove sample data fallback, use only REST Countries API
- 46ce08b fix: REST Countries API - reduced to 10 fields and added fallback
- 458d4f7 fix: naming - add space in nullable property declaration